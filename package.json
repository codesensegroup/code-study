{
  "name": "study-document-record",
  "version": "1.0.0",
  "description": "Code Sense Study document record.",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "serve": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "generate:github": "DEPLOY_ENV=GH_PAGES nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup",
    "postinstall": "patch-package"
  },
  "dependencies": {
    "@nuxt/content-theme-docs": "^0.11.0",
    "busuanzi.pure.js": "^1.0.3",
    "nuxt": "^2.15.8",
    "vue-multiselect": "^2.1.6"
  },
  "devDependencies": {
    "patch-package": "^6.4.7",
    "push-dir": "^0.4.1"
  }
}
